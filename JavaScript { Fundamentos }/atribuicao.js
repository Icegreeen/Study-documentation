const a = 7 

let b = 3 // atribuicao de valor

b += a // a = b + a / atruibicao aditiva
console.log(b) // 10

b -= 4 // b = b - 4 / atribuicao subtrativa
console.log(b) 

b *= 2 // b = b * 2 / atribuicao mutiplicativa 
console.log(b)

b /= 2 // b = b / 2 / atribuicao divisiva 
console.log(b)

b %= 2  // b = b % 2 / atribuicao modular  // % é um modulo que pega o resto da divisão de uma operação. 
console.log(b) // se for par o valor sera zero, 